# 0 号契约 · 服务于人的 Agent 宪法

> 本仓**最高、最简、统领一切**的契约。下面所有技术契约（message-canonical / send-target /
> group-metadata / moments / control-and-voice / 版本规范）都为它服务、可追溯到它。
> **所有参与的开发者（含 Agent）与人，一律遵守。**

## 五条

1. **演进，不是革命** —— AI 帮人提效、放大创造力，**叠加增强人，不掀桌、不替代人**。
   （技术史依据：能活下来的技术都是叠加——TCP/IP 没掀电话网、HTTP 没掀文档、USB 没掀串口；
   革命被回滚，演进会复利。所以"赋能不替代"不只是道德选择，是结构上更持久的策略。）
2. **人是主角** —— 人**掌配比与最终权**：设定「这件事、这段关系上，Agent 能自己跑到哪一档」，
   并**随时能收回**。人做的是**元决策**（定档、看数据、抓异常），**不是每一次点击**。
   人和 Agent 都能感知、决策、动作（同席做事），唯一不对等在**最终权归人**；结果**回交给人看**。
   （物质·信息·能量：创造与定向这类高价值、不可外包的信息行为留人，执行这类可外包的能量按**配比**
   下放 Agent。「人是主角」**不等于**「人做每一件事」——那是字面，不是里子。执行轴见下节。）
3. **赋能，不是替代** —— Agent 把活干了 = 给人**赋能**，人因此活得更有价值。
   HITL 不是给 Agent 上铐，是保证"Agent 放大的是人、不是替代人"的**结构保证**。
   （**配比可调 = 赋能的量化**：Agent 接手越多，人被放大得越多——前提是**收回权始终在人**。）
4. **积德** —— 真照顾客户、不刷屏、不操纵、做长久生意；**短期收割耗德，长期赋能积德**。
5. **信息（算料）是 AI 时代的生产资料** —— 一切 Agent 软件，本质都是对**信息**做**封装、加工、
   组织、变形、推动**：把散落的信息**封装进契约**，再让它在人与系统、系统与系统之间来回路由，
   **让人能做成更多的事**。（物质·信息·能量里，本产品搬运的是**信息**这一极；DIKW 从数据到智慧，
   统一称「信息」，也即「**算料**」——**算料是人在 AI 时代真正拥有的生产资料**；契约，就是把这份
   生产资料**标准化、可流通、可增值**的方式。）

## 人机协作配比（本宪法的执行轴 · 2026-07-26 钦定）

**一件事里人做多少、Agent 做多少，是一个可调的配比。** 人掌的是**配比本身**与**最终权**，不是每一次
点击。这根轴是本宪法从「理念」落到「做法」的**唯一刻度**——任何技术契约、任何实现，凡谈到
「自动 / 人审 / 放权」，**一律落在这根轴上说话，不得另造一套档位词汇**（各仓各造 = 隐蔽漂移）。

### 一根梯子（六档 · 从人 100% 到 Agent 100%）

| 档 | 名 | 人做什么 | Agent 做什么 |
|---|---|---|---|
| `off` | 不介入 | 全部自己来 | 不拟不发 |
| `draft` | 只拟不发 | 自己发 | 拟稿供参考 |
| `semi` | 半自动 | 点确认才发 | 拟稿 + 待放行后发 |
| `parallel` | 并行 | 也在同一条线索上发 | 自己发 · 人机同席 |
| `audit` | 事后审计 | 事后抽查内容 | 自己发 |
| `auto` | 全自动 | 只看仪表盘 | 自主发 |

- **档位是每个「场」独立的，不是全局开关。** 沿两根轴释放：**时间**（信任累积 → 往右滑）×
  **空间**（单个关系 → 一类关系 → 一个渠道 → 全域）。
- **作用域三层**：`生效档 = min(账号封顶档, 所属授权组档（多组取最小）, 会话档)`；总闸正交压顶。
  **授权组 = 显式成员制的安全边界**，**不得**从用途 / 策展标签推导——否则改标签 = 悄悄改发送权限
  = **授权静默漂移**（违反显式 gate + 留痕）。

### 四条不变式（这就是「人在回路」的机器可判定定义）

1. **档位由人设** —— Agent 不得给自己升档，**系统绝不自升**。
2. **每档都留可介入手柄 + 撤回窗 + 留痕**（谁 / 何时 / 为什么）+ 可观测。
   **没有手柄的档位不存在。**
3. **默认保守** —— 新关系 / 新场从 `off` 起；不可逆动作按下面的封顶表卡死。
4. **异常必自降** —— 不确定 / 出格 / 信号异常 / 人到场接管 → **自动收缩到 `draft`**。
   **不是 `off`**：收缩是让 Agent **停手**，不是让它**失明**——退到 `off` 连拟稿都停，
   人接手时反而没有参考。

### 正交项（不占档位）

判据：**不改变「人做多少 vs Agent 做多少」的东西，就不是这根轴上的一格。**

**撤回窗**（所有档共有的安全网）· **总闸 killswitch** · **准入条件**（双闸 / 时段 / 限频，决定能否进
`semi` 以上）· **确认粒度**（逐条 / 批量）· **拟人延迟**（节奏参数，不是审批窗）。

> **「倒计时否决窗」已废**（2026-07-26 定）：它不是协作配比的一格，是「发送前等待」的实现，
> **价值只在人在场时成立**，人不在场就退化成纯延迟。反悔需求由**撤回窗**承接——与即时通讯的原生
> 撤回（如微信 2 分钟）一致、跟人性走、人不在场也能补救。
> **这笔账诚实记下**：否决窗是**事前网**（拦在投递之前，但要人在场），撤回窗是**事后网**
> （消息已投递，撤回只能收回展示）。这是有意的取舍——用**跟人性一致、人不在场也生效**的网，
> 换掉**只在人盯着时才成立**的网；被弱化的那一格由下面的**封顶表**与**异常必自降**补齐。

### 封顶（可逆性决定档位上限）

| 动作 | 可逆性 | 档位上限 |
|---|---|---|
| 会话内发消息 | **有限可逆**（撤回窗内可撤） | 可到 `auto` |
| 一对多广播（朋友圈发布 / 群发） | **不可逆**（发出即扩散） | **封顶 `semi`** —— 永远人亲手放行 |
| 删除 / 撤销 / 解绑等破坏性写 | 不可逆 | **封顶 `semi`** |
| 首次建立关系（加好友 / 发起通话） | 不可逆（对方已感知） | **封顶 `semi`** |

不论其它档怎么调，封顶表**卡死上限**；技术契约可以更严，**不得更松**。

## 怎么落到每份技术契约

- 对外动作（send / publish / like）→ 在**人设定的协作档**内执行；**人没授权的档位下，系统永不替你发**。
  档位越低越要 dry-run → 确认 → act；**每一档都必须有可介入手柄 + 撤回窗 + 留痕**。
- 协作档 → **人手拨，系统绝不自升**；**异常必自降**（收缩到 `draft`）。
- 每个结果（诊断 / 候选 / 简报 / 已发）→ **回交给人看**。回交的**粒度随档位变**
  （逐条 → 批量 → 抽样 → 仪表盘），**但永不为零**。
- LLM → 只**向人 veto**，**永不替人越权**；模型全挂时人仍能**全手动**（LLM 从不当 gate）。

→ 一句话：**把杂活交给数字员工，把舵握在人手里——舵是「配比」这个旋钮，不是每一次划桨。**

## 红线

任何技术契约、任何实现，**不得违背本宪法**：

- **不得设计人收不回的路径** —— 判据**不是**「人有没有经手每一条」，**是**「人**是否随时能收回**」：
  **总闸可断 / 档位可降 / 动作可撤 / 全程有痕**。`auto` 是梯子的**合法终点**，
  「人被旁路」**不是**——二者的分界就在这条可收回性上，这四样缺一即违宪。
- **不得让 Agent 替人做真不可逆的决策** —— 广播 / 删除 / 建立关系按封顶表止于 `semi`，人亲手放行。
- **不得为了自动化牺牲人的决策权** —— **调档本身就是人的决策权在行使**，不是对它的牺牲；
  剥夺人**调档 / 收回**的能力，才是牺牲。
- **不得另造一套档位词汇** —— 谈自动化一律落在上面那根六档梯子上。

**违背本宪法的契约/实现，无论多高效，不予采纳。**

---

*0 号契约 · **v2**（2026-07-26 修订：条 2 改写为「掌配比与最终权」· 新增「人机协作配比」执行轴节 ·
落地四条与红线改按**可收回性**判定 · 废「倒计时否决窗」）· v1 = 2026-06 初版。
这是契约树的根。下面每份技术契约的设计与评审，都以本宪法为最终裁决依据。*
